<div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1">
      <h1><%= @list.name %></h1>
      <table class="table table-bordered">
        <tbody>
          <tr>
            <%= image_tag @list.photos %>
            <td><%= @list.name %></td>
          </tr>
        </tbody>
      </table>

      <%= link_to "See all lists", lists_path %>
    </div>
  </div>
